import React from "react";
import { Box } from '@mui/material';

const NOIs = () => {
  return (
    <Box sx={{pt:20, display: { xs: "none", sm: "block" } }}>
      NOIS PAGE
    </Box>
  );
};

export default NOIs;